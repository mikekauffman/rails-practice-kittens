<%= form_for [@kitten, @categorization] do |f| %>
    <% if @categorization.errors.any? %>
        <%= @categorization.errors.full_messages.last %>
    <% end %>
    <%= f.collection_select :category_id, @categories, :id, :name, {include_blank: true} %>
    <%= f.submit "Add Category" %>
<% end %>
